---
import BaseLayout from './BaseLayout.astro';
const { frontmatter } = Astro.props;
---
<BaseLayout title={frontmatter.title}>
  <p>{frontmatter.description}</p>
  <p>Published on: {frontmatter.publishDate.toDateString()}</p>
  <slot />
</BaseLayout>
