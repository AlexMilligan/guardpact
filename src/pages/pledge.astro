---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Card, CardGrid } from '@astrojs/starlight/components';
import GuardPactLogo from '../components/GuardPactLogo.astro';
---
<BaseLayout title="Take the Pledge" description="Join a movement of Americans who believe government should be a shield, not a sword. Add your name to the Guard Pact.">
<div class="hero">
  <GuardPactLogo width={180} class="mb-8" />
  <h1>Take the Pledge. Join the Guardians.</h1>
  <p class="lead">
    This isn't about party or ideology. It's a commitment to a government that is clear, accountable, and built to serve.
  </p>
  <p class="lead">
    By adding your name, you are sending a clear message: you want a government that adheres to the <a href="/01-hlg">5 High-Level Goals</a> that over 90% of Americans agree on.
  </p>
</div>

<CardGrid stagger>
  <Card title="Your Pledge" icon="document">
    "I want a government built on evidence, not ideology. I support the <strong>Guard Pact's <a href="/01-hlg">5 High-Level Goals</a></strong> and its core mechanics for accountability: mandatory sunset clauses, <a href="/04-reference/circuit-breaker-catalog">circuit-breakers</a> on new restrictions, <a href="/02-essential-points/18-prediction-juries">citizen juries</a>, and competitive <a href="/03-state-labs">state labs</a>."
  </Card>
  <Card title="What Happens Next" icon="rocket">
    - <strong>You become a Guardian.</strong> Your first name and state are added to our public counter.
    - <strong>You stay informed.</strong> We'll send brief, high-signal updates on major milestones. No spam.
    - <strong>We build leverage.</strong> At critical mass, we deliver the Pact to Congress and all presidential candidates as a clear mandate from the American people.
  </Card>
</CardGrid>

<div class="pledge-form-wrapper not-prose">

<h2 class="text-2xl font-bold mb-6 text-center">Add Your Name — It Takes 10 Seconds</h2>

<form
  name="guard-pact-pledge"
  method="POST"
  data-netlify="true"
  netlify-honeypot="bot-field"
  action="/pledge-success"
  class="pledge-form max-w-lg mx-auto"
>
  <input type="hidden" name="form-name" value="guard-pact-pledge" />
  <p class="hidden"><label>Don’t fill this out if you’re human: <input name="bot-field" /></label></p>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
    <div>
      <label for="firstName" class="block font-medium mb-1">First Name <span class="text-red-600">*</span></label>
      <input
        type="text"
        id="firstName"
        name="firstName"
        required
        class="w-full p-2 border rounded"
        placeholder="Jane"
      />
    </div>
    <div>
      <label for="lastName" class="block font-medium mb-1">Last Name <span class="text-red-600">*</span></label>
      <input
        type="text"
        id="lastName"
        name="lastName"
        required
        class="w-full p-2 border rounded"
        placeholder="Doe"
      />
    </div>
  </div>

  <div class="mb-4">
    <label for="email" class="block font-medium mb-1">Email <span class="text-red-600">*</span></label>
    <input
      type="email"
      id="email"
      name="email"
      required
      class="w-full p-2 border rounded"
      placeholder="you@example.com"
    />
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
    <div>
      <label for="state" class="block font-medium mb-1">State <span class="text-red-600">*</span></label>
      <input
        type="text"
        id="state"
        name="state"
        required
        class="w-full p-2 border rounded"
        placeholder="California"
      />
    </div>
    <div>
      <label for="zip" class="block font-medium mb-1">Zip Code</label>
      <input
        type="text"
        id="zip"
        name="zip"
        class="w-full p-2 border rounded"
        placeholder="90210"
      />
    </div>
  </div>

  <button type="submit" class="w-full bg-blue-600 text-white font-bold py-3 px-4 rounded hover:bg-blue-700 transition-colors">
    Sign the Pledge
  </button>
</form>
</div>
<style>
/* Keeping original styles */
.hero {
  padding: 4rem 0 2rem;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.hero h1 {
  font-size: 3rem;
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: 1rem;
}
.hero .lead {
  font-size: 1.25rem;
  max-width: 60ch;
  margin: 0 auto 1rem;
}
.pledge-form-wrapper {
  margin-top: 4rem;
  margin-bottom: 4rem;
  padding: 3rem;
  border: 1px solid var(--sl-color-gray-5);
  border-radius: 0.5rem;
  background-color: var(--sl-color-gray-6);
}

.pledge-form label {
  color: var(--sl-color-text-accent);
}

.pledge-form input {
  background-color: var(--sl-color-gray-7);
  border-color: var(--sl-color-gray-5);
  color: var(--sl-color-text);
}
.pledge-form input:focus {
    border-color: var(--sl-color-accent);
    outline: 2px solid var(--sl-color-accent);
}
.pledge-form button {
    background-color: var(--sl-color-accent);
}
.pledge-form button:hover {
    background-color: var(--sl-color-accent-high);
}

@media (max-width: 600px) {
    .pledge-form-wrapper {
        padding: 2rem 1.5rem;
    }
}
</style>
</BaseLayout>
